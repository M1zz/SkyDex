# SkyDex TODO

## 완료
- [x] 빌드 에러 수정 — `DialView.swift`의 `cos`/`sin` 오버로드 모호성 (Double/CGFloat 혼용).
      각도 계산을 `Double`로 통일하고 마지막에만 `CGFloat`로 변환. arm64 슬라이스에서만
      터지던 문제라 x86_64만 보면 놓치기 쉬움. *(DialView는 이후 제거됨)*
- [x] `xcodebuild ... clean build` (iphonesimulator) 통과 확인.

## 완료 — 모자이크 전환 (2026-08-10)
- [x] 판정 제거. `CollectEngine` / `CollectOutcome` 삭제. 찍으면 무조건 저장.
- [x] `SkyColorExtractor.skyColor(from:)` — nil을 안 돌려주는 폴백 체인 추가.
      (밝은 클러스터 → 아무 클러스터 → 표본 평균 → 중간 회색)
- [x] 사진 저장. `SkyEntry`에 썸네일(인라인 360px) + 원본(externalStorage 1280px).
- [x] 원형 다이얼 → 격자 모자이크. `DialView` / `DotDetailView` 삭제,
      `MosaicView` / `PhotoDetailView` 추가.
- [x] 정렬은 날짜가 아니라 **시각**(`SkyEntry.minuteOfDay`). 하루 한 바퀴 그림이 됨.
- [x] 분모 제거. `N / 37` → "모은 하늘 N장".
- [x] 정사각 1:1 카메라. `UIImagePickerController` → AVFoundation `SquareCameraView`.
      프리뷰도 정사각, 저장 파일도 같은 정사각.
- [x] 예전 저장소 보정 `SkyEntry.repairLegacyRows(in:)` — 기존 행의 `minuteOfDay` 되채움.
- [x] Info.plist 카메라 사용 설명 갱신 (더 이상 "저장하지 않습니다"가 아님).
- [x] 경고 없이 clean build 통과. 시뮬레이터에서 모자이크/상세 화면 육안 확인.

## 크래시 대응 (2026-08-10)
"실행했을 때 죽기도 한다" 제보. 시뮬레이터에서는 **재현 실패** — 저장소 상태 4종
(빈 저장소 / 사진 34장 / 예전 스키마 업그레이드 / 손상된 저장소) × 총 18회 실행 전부 생존.
재현은 못 했지만 근거가 확실한 결함 세 가지를 고침:

- [x] **들여오기 경로 메모리 폭증.** 12MP 입력에 순간 +53MB. orientation 플래그가 붙은
      실제 촬영본은 `orientedUp()`이 원본 크기 비트맵을 한 겹 더 만들어 더 나빠짐.
      실기기에서 jetsam 강제 종료 → 사용자 눈에는 크래시.
      `UIImage` 경유를 버리고 `Data` → `CGImageSourceCreateThumbnailAtIndex`로 교체.
      **측정: +53MB → +1.5MB**, 40ms, 결과는 그대로 1280×1280.
- [x] **셔터 조기 활성화.** `startRunning()`을 큐에 넣자마자 `.running`으로 표시해서,
      그 틈에 셔터를 누르면 활성 커넥션 없이 `capturePhoto` 호출 →
      `NSInvalidArgumentException` → Swift에서 못 잡고 즉사. 시뮬레이터에는 카메라가
      없어 절대 안 걸리는, 실기기 전용 경로. 세션이 실제로 돌 때까지 기다리도록 수정하고,
      촬영 직전 검사도 세션 큐 위로 옮김.
- [x] **`flip()` 실패 시 입력 없는 세션.** 전환 실패하면 원래 카메라를 되돌리도록 수정.
- [x] **컨테이너 생성이 `fatalError`로 끝나던 것.** 직접 열고, 안 열리면 저장소를
      `.unreadable-<시각>`으로 치운 뒤 새로 시작. 저장소를 일부러 깨뜨려 검증 —
      죽지 않고 뜸.
- [x] 예전 스키마 보정을 `RootView.task` → `SkyDexApp.init`으로 이동
      (`@Query`가 살아나기 전에 끝나도록).

### 원인 규명이 안 끝난 부분
- [ ] **어디서 죽는지 아직 모름.** 위 셋 중 하나였을 가능성이 높지만 확증 없음.
      또 죽으면 Xcode 콘솔 마지막 줄과 어떤 동작에서 죽었는지 기록할 것.
- [ ] 실기기 카메라 경로 자체 — 시뮬레이터에 카메라가 없어 프리뷰/촬영을 못 돌려봄.

## 완료 — 첫 하늘이 초라해 보이던 것 (2026-08-11)
타일 폭이 76~140pt 고정이라, 100장을 전제로 짠 격자에 한 장만 놓이면 왼쪽 위에 우표만 한
게 하나 떠 있고 아래에 "모은 하늘 1장"이 붙어 있었음.

- [x] 열 수를 개수에 연동. 1→1열(화면 폭 가득), 2→2, 3→3, 4→2, 5–9→3, 10–20→4, 21+→가변.
      단계 경계를 딱 떨어지는 수(4=2×2, 9=3×3, 20=5×4)에 맞춤.
- [x] 9장까지는 **앨범** — 간격 10, 모서리 14, 타일마다 날짜·시각. 10장부터 모자이크.
- [x] "모은 하늘 N장"과 순서 설명은 10장부터만 표시.
- [x] 앨범 구간은 세로 가운데 정렬, 모자이크는 위에서부터 자람.
- [x] 1 / 3 / 5 / 10 / 12장 상태 시뮬레이터에서 육안 확인.

## 완료 — 구슬 판으로 전환 (2026-08-11)
시안 6개를 렌더링해 보여드리고 시안 4(구슬)를 고르심. 이어서 "다 빈칸으로 두고
기본점으로부터 색을 채워서 모아나가게, 그라데이션이 되도록 색상값 조정" 요청.

- [x] 팔레트 재설계. 실제 시각(시민박명·일출·정오·골든아워·일몰·잔광)에 앵커를 박고
      CIELAB에서 보간 → 48색. 자정에서 시작해 자정으로 닫힘. RGB 보간은 새벽 파랑이
      탁한 회색을 지나가서 안 됨.
- [x] `Palette`/`SkyTarget` → `SkyBoard`/`SkySlot`. 12섹터×링 → 48자리.
      구간별 구슬 수를 6의 배수로 맞춰 격자 줄이 안 어긋나게 함
      (한밤6·여명6·낮12·늦은오후6·노을12·밤6).
- [x] `MosaicView` → `BoardView`. 빈 구슬은 제 색 28% + 테두리, 채운 구슬은 찍은 색.
- [x] 판 전체가 스크롤 없이 한 화면에. 구슬 지름을 남는 높이에서 역산.
- [x] 이미 채운 자리를 다시 찍으면 갱신 (`@Query` 오름차순 + 딕셔너리 덮어쓰기).
- [x] 빈 자리 탭 → 시간대·구간·"색이 맞아야 할 필요는 없습니다" 안내 시트.
- [x] `SkyEntry.sector` → `slotID`, 마이그레이션 보정 플래그 갱신.
- [x] `DeltaE.swift` 삭제 (더 이상 아무것도 색을 비교하지 않음). 백업은 스크래치패드.
- [x] 빈칸 표현 3안 × 채움 4단계 = 12컷 렌더링, 비교 페이지로 전달.

### 판에서 글자 제거 (2026-08-11)
"쓸데없는 글자 다 지워줘, 시간도 필요 없어, 눈으로 보면 다 보여."

- [x] 구간 이름·시각 범위 라벨 제거. `SkyBoard.bands` 그룹핑도 삭제 (쓰는 곳이 없어짐).
- [x] `모은 하늘 N` 카운트 제거.
- [x] 상단 내비게이션 바 숨김. 탭 바가 이미 어느 화면인지 말해줌.
- [x] 밴드별 섹션 → 단일 8×6 격자, 간격 균일, 세로 가운데 정렬.
      라벨이 빠진 만큼 구슬이 커짐.
- [x] 0 / 1 / 12 / 40칸 육안 확인.

### 한 줄 (2026-08-11)
- [x] 사진 상세에서 한 줄을 맨 아래 → 날짜 바로 아래로.
- [x] `.title3`로 키우고 테두리 없는 plain 스타일로. 화면에서 가장 큰 글씨.
- [x] "한 줄" 라벨 제거 — placeholder가 이미 같은 말을 함.
- [x] 비어 있으면 `@FocusState`로 키보드 자동. 이미 쓰여 있으면 포커스 안 뺏음.
      시트 등장 애니메이션과 안 싸우도록 450ms 뒤에 포커스.
- [x] 키보드 툴바에 완료 버튼. 닫기는 포커스를 내리고 나감.

### 모은 것 / 안 모은 것 구분 + 지금 시각 표시 (2026-08-11)
- [x] 빈 자리를 채움 → **가운데가 빈 링(도넛)** 으로. 형태로 갈리니 색이 비슷해도 안 헷갈림.
      크기 차이 안은 사용자가 반려 ("크기를 다르게 하는 건 별로").
- [x] 투명도만 조절한 안도 렌더링해 비교 → 도넛이 명확히 우세.
- [x] 지금 시각의 자리 표시. 그 자리 링만 불투명도 1.0, 바깥에 테두리 원이 겹침.
      `TimelineView(.periodic(by: 60))`이라 시계를 따라 움직임.
- [x] 빈 자리 시트의 원도 같은 도넛으로 맞춤.

### 다 채우면 어떻게 되나 — C안 (2026-08-11)
"48개를 다 찍으면 그 다음은?" → 확인해 보니 완성이라는 개념이 코드에 아예 없었고,
그냥 조용히 덮어쓰기만 하고 있었음. 선택지 4개 제시 → C(판이 세밀해짐) 선택.

- [x] 레벨 도입. 48 → 96 → 192. 레벨 2에서 멈춤 (그 위는 구슬이 겨냥 불가).
- [x] **색을 표에서 곡선으로.** 하드코딩 48색 배열을 버리고 앵커 + CIELAB 보간을
      앱 안으로. 어떤 해상도에서든 같은 하루를 샘플링. `RGB(_ lab:)` 역변환 추가.
- [x] `SkyEntry.slotID` 제거. 해상도가 바뀌면 저장된 자리 번호가 틀려짐.
      분만 저장하고 자리는 판에 물어봄.
- [x] 레벨은 도달한 최고값으로 잠금(`@AppStorage`). 사진 지운다고 판이 거칠어지지 않음.
- [x] 판이 갈라지는 순간에만 시트 1회. "48칸이 96칸이 됩니다."
      안 그러면 완성 직후 판이 반쯤 비어 보여서 데이터 손실로 읽힘.
- [x] 열 수도 레벨을 따라감 (6 → 8 → 12). 여전히 한 화면에 들어옴.
- [x] 상세/기록의 라벨을 자리 범위 → 구간 이름으로. 범위는 레벨 따라 변해서 금방 낡음.
- [x] 레벨 0(40칸) / 쪼개짐 시트 / 레벨 1(96칸 중 48) / 레벨 2(192칸 중 96) 육안 확인.

### 빈칸은 무채색 — 안 3 (2026-08-11)
"C로 가줘" → 빈칸 표현 3안 중 세 번째(무채색) 선택.

- [x] 빈 자리를 제 시각 색 → 무채색 링으로. `.emptySlot` 토큰 하나로 판·빈자리 시트·
      쪼개짐 시트가 같은 회색을 씀.
- [x] 도넛 형태는 유지. 이제 형태와 색 두 겹으로 갈림.
- [x] 도넛 모양 확정 — 사용자 "가운데 구멍 뚫린 모양 너무 좋고".
- [x] **라이트/다크에서 같은 회색.** 처음엔 `.label` 옅게(라이트 0.28 / 다크 0.2)로 갔는데,
      배경에서 파생하면 모드마다 밝기 방향이 뒤집혀서(종이보다 어두움 ↔ 밝음) 두 모드가
      다른 디자인이 됨. **L\*50 고정 회색**(0.46/0.46/0.48)으로 교체 — 흰 종이와 검은
      종이에서 같은 거리.
- [x] 값 후보 0.56 / 0.44 를 30장 상태로 라이트·다크 다 찍어 비교. 0.56은 어두운 판에서
      빈 링이 거의 검은 밤하늘 구슬을 이겨버림. L*50이 양쪽 다 견디는 지점.
- [ ] 남은 모드 차이는 종이(도넛 구멍 안쪽 포함)뿐. 없애려면 판 배경을 두 모드에서
      고정해야 하는데 탭 바·촬영 바와 어긋남. 안 함.

### 빈 자리에 색을 흐릿하게 돌려놓음 (2026-08-11)
"안 찍은 색상의 하늘을 흐릿하게라도 위치 잡아서 보여줬으면 해." 무채색 회색을 버리고
**Lab에서 흐리게 한 제 시각 색**으로. 도넛과 라이트/다크 일치는 유지.

- [x] `SkyBoard.ghost(of:)` — 밝기는 L*52 쪽으로 당겨 제 밝기 32%만, 채도는 38%만 남김.
      투명도로 흐리게 하면 검은 배경에서 한밤 링이 사라져서(모드마다 방향이 뒤집힘) 안 됨.
- [x] `SkySlot.ghost` 추가, 판 만들 때 같이 계산 (레벨별로 미리 만들어두는 구조 그대로).
- [x] 판·빈 자리 시트·쪼개짐 시트 전부 ghost 색으로. `.emptySlot` 회색 토큰 삭제.
- [x] 빈 판에서 하루의 모양이 보임 — 위는 슬레이트, 낮은 파랑, 아래는 탄/녹슨색.
      라이트와 다크가 거의 같은 그림.
- [x] 0(라이트/다크) / 12(라이트/다크) / 48→쪼개짐 시트 / 레벨 1 육안 확인.
      12장 상태에서 찍은 구슬(주황·녹슨색·자주)이 흐린 링들 위로 확실히 뜸.
- [x] 지금 시각의 자리만 제 색 유지 — 판에서 색은 "모았다" 또는 "지금" 둘 중 하나가 됨.
- [x] 쪼개짐 시트 그림: 12자리를 전부 빈 것으로 그리던 것을 한 칸 띄고 채운 것으로.
      시트가 부정하려는 상실을 그림이 그리고 있었음.
- [x] 0(라이트/다크) / 12(라이트) / 30(다크) / 48→쪼개짐 시트 / 레벨 1 육안 확인.
      시드는 `scratchpad/seed.py`로 스토어에 직접 씀 (시계가 정하는 자리라 UI로는
      한 칸밖에 못 채움).
- [ ] 빈 자리 탭 시트의 큰 링은 스크린샷 못 찍음 — 시뮬레이터를 CLI로 탭할 방법이 없음.
      판과 같은 `.emptySlot` 토큰이라 코드상 동일.
### 사용자 결정 대기
- [ ] 야간 6칸 문제 — 00–05시는 깨어 있어야 채워짐. 판의 1/8이 계속 빌 것.
      압박으로 느껴지면 야간 구간 축소 또는 제거. 빈칸이 무채색이 된 뒤로는 그 여섯 칸이
      회색으로 계속 남아 더 눈에 걸릴 수 있음.

### 촬영 바의 라이브러리 버튼 → 화면 전환 버튼 (2026-08-11)
- [x] 촬영 바에서 `photo.on.rectangle` 버튼 제거. 라이브러리는 카메라 화면 안에
      그대로 있음 (`SquareCamera.swift`의 `onPickFromLibrary`) — 기능이 아니라
      입구 하나가 없어진 것.
- [x] 그 자리에 기록으로 넘어가는 버튼(`list.bullet`).
- [x] 기록 쪽 오른쪽 위에도 판으로 돌아오는 버튼(`circle.grid.3x3.fill`). 한쪽만
      있으면 토글이 아니라 일방통행.
- [x] 선택 상태를 `RootView`의 `Screen`으로 올리고 두 화면에 `@Binding`으로 내림.
      탭 안에서는 자기 탭 바의 선택을 만질 수 없음.
- [x] `TabView(selection:)` + `.tag()`가 실제로 먹는지 확인 (임시 훅으로 전환시켜
      스크린샷 → 기록 화면으로 넘어감, 훅 제거 후 재빌드).
- [x] 두 버튼이 탭 바와 아이콘·목적지가 겹쳤음 → 아래 2026-08-12 항목에서 탭 바 제거로 해소.

### 판 고정 · 나이 · 피드 · 탭 바 제거 (2026-08-12)
- [x] **레벨 제거.** 48 → 96 → 192 전부 삭제. `maxLevel`/`slots(level:)`/`columns(level:)`/
      `level(forMinutes:)`/`@AppStorage("skydex.boardLevel")`/`BoardSplitSheet`/
      `SkySlot.level` 다 없앰. 판은 48칸 6×8 고정.
- [x] **채운 칸이 나이를 먹음.** `SkyEntry.freshness(at:)` — 7일 그대로, 이후 56일에 걸쳐
      감소. `SkyBoard.faded(_:freshness:)`가 그 칸의 빈 색 쪽으로 62%까지만 당김.
      다시 찍으면 즉시 복구. `TimelineView(.periodic(by: 60))`이 이미 있어 시계를 따라감.
- [x] 최대 흐림 0.8 → 0.62. 0.8은 오래된 구슬이 빈 링과 색이 거의 같아져서 형태 혼자
      "모았다"를 짊어짐. 전부 신선 / 전부 120일 두 장을 찍어 비교하고 정함.
- [x] **타임라인 피드** (`TimelineFeedView`). 최근이 위. 채운 구슬을 누르면 **그 사진 위치로
      스크롤된 상태**로 열림 (`ScrollViewReader`). 전체 화면(`fullScreenCover`).
- [x] 피드 카드에서 사진을 누르면 상세도 전체 화면. 기록의 행도 시트 → 전체 화면.
      빈 자리 안내만 시트로 남김 (자리에 대한 쪽지지 사진이 아님).
- [x] `PhotoCache` 추가 — 개수 대신 **바이트**(48MB)로 상한. 1280 정사각 한 장이 6.5MB라
      피드가 개수 상한을 만나면 기가바이트로 자람. 예전 jetsam 사고와 같은 종류.
- [x] **탭 바 제거.** `RootView`가 껍데기가 됨 — 화면 전환 + 촬영 + 저장 + `landed`.
      두 화면에 같은 바, 같은 자리. (전엔 기록으로 갈 버튼은 아래, 판으로 올 버튼은 위였음.)
- [x] 기록에서 찍으면 판으로 데려감 — 기록에는 펄스를 보여줄 구슬이 없음.
- [x] 육안 확인: 48칸 판 / 전부 신선 / 전부 120일 / 0~120일 섞임 / 피드(스크롤 위치까지) /
      기록 화면의 새 바. 피드와 화면 전환은 임시 훅으로 상태를 바꿔 확인하고 훅 제거.

### 아이콘 + 촬영 바 (2026-08-12)
- [x] 앱 아이콘. 도넛 하나에 하루 한 바퀴, 자정이 12시 방향에서 시계 방향.
      색은 앱과 같은 앵커·CIELAB 보간(`Tools/MakeIcon.swift`, CoreGraphics로 1024 렌더).
- [x] 한 바퀴를 분이 아니라 **자리(48칸)** 로 돌게 고침. 분으로 돌리면 노을이 얇은 조각이
      되어 판의 그림이 아니게 됨. 첫 렌더에서 눈으로 확인하고 바꿈.
- [x] 라이트/다크 두 장 (`AppIcon.png`, `AppIcon-Dark.png` + `luminosity` appearance).
      다크는 밝기를 중간으로 들어올리고 채도도 같은 비율로 — 밝기만 올리면 한밤이 회색.
- [x] `Tools/`가 동기화 그룹(`SkyDex/`) 밖이라 앱 타깃에 안 들어감 — 빌드로 확인.
- [x] 홈 화면에서 처음 이상한 아이콘이 뜬 건 스프링보드 캐시 잔상. 시뮬레이터 재시작 후 정상.
      다크 변형은 `assetutil`로 `Assets.car`에 `UIAppearanceDark`로 들어간 것 확인
      (시뮬레이터 홈 화면은 다크에서도 라이트 아이콘을 계속 보여줬음).
- [x] 촬영 바에서 `.background(.bar)` 제거 — 탭 바 시절의 흔적이라 판에 이음선만 그었음.
      대신 페이지 색 그라데이션 스크림.
- [x] "하늘 찍기" 글자 제거, 둥근 셔터(64pt)만. 화면 전환 버튼은 오른쪽 끝에 원형으로.
      셔터는 overlay 정렬이라 아이콘이 바뀌어도 중앙선에서 안 움직임.
- [x] 기록도 판과 같은 종이(`scrollContentBackground(.hidden)`). 재질을 뺐더니 목록 행이
      버튼 뒤로 지나가 글자를 가렸음 → 스크림 + 같은 종이로 해결. 스크린샷으로 확인.

### 기준색을 실제 태양에 맞춤 (2026-08-12)
"자리의 기준색을 실제 일출·일몰 시각에 맞추는 것". WeatherKit은 필요 없음 — 위도·경도와
날짜만으로 오프라인 계산.

- [x] `ColorScience/Sun.swift` — NOAA 태양 위치. 시민박명·일출·정오·일몰·시민박명 끝을
      분(minute of day)으로. 극지에서 일출이 없으면 nil.
- [x] `Models/SkyDay.swift` — 앵커를 시계 분값 → **순간(+오프셋)** 으로. 하루마다 풀어서
      앉히고, 겨울에 앵커가 겹치거나 여름에 자정을 넘으면 단조 증가로 clamp.
      풀 수 없으면(백야·극야) 예전 고정 표로 폴백.
- [x] `SkySlot`에서 색 제거 — 자리는 시계의 토막일 뿐. 색은 `SkyDay`에 물어봄.
      48칸 경계는 안 움직임(사진 분류가 매일 같아야 함).
- [x] `Support/Place.swift` — CoreLocation 한 번, km 정확도, `UserDefaults` 캐시,
      기본값 서울. `SkyEntry`에는 절대 안 씀. 거부해도 동작.
- [x] `@preconcurrency CLLocationManagerDelegate`로 분리 — MainActor 격리 경고가
      Swift 6에서 에러가 됨.
- [x] Info.plist 위치 사용 설명 추가(두 컨피그 다). 처음엔 다시 빌드를 안 해서 권한
      대화상자가 안 떴음 — 설명이 없으면 iOS가 요청을 조용히 무시함.
- [x] **검증**: 서울 1월 1일 07:47/17:24, 7월 1일 05:14/19:57 — 공표값과 분 단위로 일치.
      4월·10월은 내 기억값과 10분 안팎 달랐는데 일조 시간 교차검증으로 계산이 맞음을 확인.
      툴룸소 하지 → `hasRiseAndSet: false` → 고정 표 폴백. (`scratchpad/SunCheck.swift`)
- [x] 앱에서 판 렌더링 + 권한 문구 육안 확인.
- [ ] 아이콘은 여전히 고정 표를 씁니다 — 아이콘에 위치가 있을 수 없으니 의도된 것.

### 날씨 큐레이션 (2026-08-12)
번들 ID `com.leeo.SkyDex`, 엔타이틀먼트는 사용자가 Xcode에서 먼저 넣어두심.

- [x] `Models/SkyForecast.swift` — 시간당 구름량·강수량 24개. WeatherKit을 import하지 않는
      순수 데이터라 다른 제공자로 갈아끼울 수 있고, 검증 스크립트에서도 만들 수 있음.
- [x] `Support/SkyWeather.swift` — WeatherKit `.hourly(startDate:endDate:)`로 오늘 하루치.
      한 시간에 한 번 + 5km 넘게 이동했을 때만 호출. 12시간 미만 받으면 폐기(0으로 채우면
      "맑음"이라고 거짓말하게 됨). 빠진 시간은 이웃 값으로 메움.
- [x] `SkyDay.clouded(_:cloud:rain:)` — 채도를 거의 전부 깎고(×0.15), 밝기는 원래 밝았던
      만큼만 밝은 회색으로 당김. 비는 밝기를 더 낮춤. 구름 덮인 자정이 밝아지지 않는 게 핵심.
- [x] 출처 표기: 예보가 쓰이는 동안에만 빈 자리 시트에 "Apple Weather" + 법적 링크.
      같은 시트 문구도 "보통" → "오늘 이 시각은 대략"으로.
- [x] 찍은 구슬은 안 건드림. 날씨는 예상에만 관여.
- [x] 엔타이틀먼트가 바이너리에 들어간 것 확인(`__entitlements` 섹션).
- [x] 색 모델 숫자 검증: 정오 맑음 `#2876BA` → 흐림 `#878D98` → 비 `#696C72`,
      노을 `#B45835` → 흐림 `#9A8983`, 자정은 거의 불변. (`scratchpad/weathercheck`)
- [x] 합성 예보(오전 맑음 → 오후 흐림 → 16시 비)를 임시로 넣어 판 육안 확인 후 훅 제거.
- [ ] **실제 응답은 미확인.** WeatherKit이 답을 주는 건 App ID에 서비스가 켜진 서명 빌드에서만
      확인 가능. 실기기에서 흐린 날/맑은 날 판이 다른지 봐야 함.

### 데이터 유실 사고 (2026-08-12)
번들 ID를 `com.yourname.SkyDex` → `com.leeo.SkyDex`로 바꾸자 iOS가 새 컨테이너를 만들어
판이 빈 상태로 뜸. **내가 번들 ID를 바꾸라고 하면서 컨테이너가 따라오지 않는다는 말을
안 했음.**

- [x] 폰에 예전 앱이 남아 있는 것 확인(`devicectl device info apps`) — 지워지지 않았음.
- [x] `devicectl device copy from`으로 예전 컨테이너 회수 → 3장 전부 확인
      (8/11 두 장, 하나는 "오늘 하루도 수고했다!" 메모, 8/12 한 장, 원본 사진 2개).
- [x] 새 앱 컨테이너를 먼저 백업(`scratchpad/newbackup`) 후, WAL 체크포인트하고
      store·-wal·-shm·숨은 `.default_SUPPORT`까지 밀어넣음.
- [x] 다시 꺼내서 3행·메모·원본 사진 확인.
- [x] README 맨 앞에 "번들 ID 바꾸지 말 것" + 복구 절차 기록.
- [ ] **아직 부족함.** 위는 사고 대응이지 예방이 아님. 데이터가 컨테이너 하나에만 있는 한
      같은 종류의 사고는 또 가능함. 다음 중 하나가 필요:
      (a) 내보내기/불러오기 파일 — 사용자가 들고 있을 수 있는 백업. UI 자리 결정 필요.
      (b) CloudKit 동기화 — 자동이지만 엔타이틀먼트·스키마 제약(전부 옵셔널/기본값).
      (c) 촬영 시 시스템 사진 앨범에도 저장 — 사진은 살지만 판·메모는 못 살림.

### 애니메이션 (2026-08-12)
- [x] 화면 전환 — 기록은 오른쪽에 있으므로 오른쪽에서 들어오고 오른쪽으로 나감(스프링).
      바는 전환 밖에 있어 안 움직임.
- [x] 구슬이 채워질 때 링 → 원으로 자라남(`transition(.scale(0.55) + .opacity)`).
      `value: entry?.uuid`로 키를 걸어 1분마다 오는 재드로에서 48개가 다 튀지 않게 함.
- [x] 지금 시각 표시가 옮겨갈 때 페이드(0.4s) — 분이 바뀔 때 튀지 않음.
- [x] 셔터·전환 버튼에 누름 반응(0.9배 스프링). `BarButton` 스타일로 둘이 같은 방식으로 답함.
- [x] 판이 처음 열릴 때 한 번만 구슬이 순서대로 나타남(칸당 8ms). 플래그는 `RootView`가
      들고 있어 기록에서 돌아올 때 다시 재생되지 않음.
- [x] 전환 버튼 글리프는 `contentTransition(.symbolEffect(.replace))`로 뒤집힘.
- [x] 육안 확인: 판 열림(1.4초에 완료).

### 피드를 자리 단위로 (2026-08-12)
"왜 모든 곳에 모든 사진이 다 나와? 구분된 사진만 나와야지."

- [x] `TimelineFeedView(startAt:)` → `TimelineFeedView(slot:)`. `@Query`에 술어를 걸어
      그 자리의 분 범위(`startMinute ..< endMinute`)만 최근 순으로.
- [x] 누른 구슬의 사진이 곧 그 자리의 최신이라 맨 위에서 열림 → `ScrollViewReader` 제거.
- [x] 제목에 "구간 · 시각 범위"를 넣어 어느 구분인지 말하게 함.
- [x] 같은 자리에 4장을 넣고 확인 (제목 "노을 · 18:00–18:15", 그 4장만 나옴).
- [x] 피드와 기록의 중복 해소 — 피드는 한 자리의 역사, 기록은 전체 시간순.

### 한 마디를 카드로 (2026-08-12)
"한마디를 남기고 싶은데 좀 더 남기고 싶은 형태가 되었으면 좋겠어."

- [x] 사진 + 한 줄을 **한 장의 카드**로. 사진이 카드 위쪽, 글이 같은 면 아래쪽
      (`secondarySystemBackground`, 모서리 26). 구분선·폼 느낌 제거.
- [x] 빈 자리 문구를 사용자 표현대로: "이 하늘을 보면서 들었던 생각, / 하고 싶은 한 마디".
      `TextField` 프롬프트는 한 줄로 잘려서(스크린샷으로 확인) 직접 그리는 `Text`로 바꿈.
- [x] 카드 아래쪽 절반 전체가 탭 대상 → 커서. 커서 색은 그 하늘의 색.
- [x] 시각·구간·헥스는 카드 밖 아래로, `.caption`으로 내림.
- [x] **찍은 직후 카드가 자동으로 열림** (카메라 닫히는 애니메이션과 안 싸우도록 450ms 뒤).
      판에서 구슬을 찾아 눌러야 하는 한 줄은 안 쓰이는 한 줄.
- [x] 빈 카드 / 쓰여 있는 카드 두 상태 육안 확인.
- [x] 글이 있으면 "닫기" → **초록 원 + 체크**로 바뀜(스프링 전환). 누르면 `context.save()`로
      실제 커밋 + 성공 햅틱. 공백만 쓴 경우는 안 바뀜(trim 검사).
- [ ] 자동으로 열리는 게 과한지는 써 봐야 앎. 싫으면 `RootView.save()`의 마지막 Task 한 덩이만
      지우면 됨.

### 예보를 문장으로 — TipKit (2026-08-12)
"웨더킷으로 인해 얻어온 인사이트나 정보가 있다면 팁킷으로 보여줘."

- [x] `Models/SkyInsight.swift` — 하루치 구름·비 + 태양 시간표에서 **한 문장**을 고름.
      먼저 참인 것이 이김: ① 지금 맑고 나중에 비 ② 지금 비, 나중에 갬 ③ 종일 흐림
      ④ 남은 시간 중 가장 맑은 때. 지나간 시간은 절대 언급 안 함.
- [x] `Views/TodaysSkyTip.swift` — `Tip` 구현. 내용은 인스턴스에 실음(매일 문장이 바뀜).
      TipKit은 타입으로 식별하니 빈도 제한은 그대로 걸림.
- [x] `Tips.configure([.displayFrequency(.daily)])`를 `SkyDexApp.init`에.
- [x] 판이 아니라 **셔터에 매달아** popover로. 판에는 글자를 상주시키지 않는다는 규칙 유지.
- [x] iOS 17의 `popoverTip`은 옵셔널 팁을 안 받음(iOS 26 오버로드로 잡혀 컴파일 에러).
      `popoverTip(ifPresent:)` 확장으로 호출부에서 갈랐음.
- [x] 합성 예보 + `showAllTipsForTesting()`으로 육안 확인 후 훅 제거.

### 태양 시각 버그 — 하루를 벗어난 값 (2026-08-12)
팁 첫 스크린샷에 **"일몰은 36:07"** 이 찍혀서 발견.

- [x] 원인: 시뮬레이터 위치(쿠퍼티노) + 시계(서울)라 solarNoon이 1748분. 계산은 맞고
      값은 쓸모없는데 아무도 안 막고 있었음. 실기기에서도 시간대를 손으로 맞춰두고
      이동하면 같은 일이 생김.
- [x] `SunTimes`에서 0–1440 밖의 값은 `nil`로 버림 → `hasRiseAndSet`이 false가 되고
      판은 고정 램프로, 팁은 일몰 문구를 생략.
- [x] 검증: 쿠퍼티노+서울시계 → nil / 쿠퍼티노+현지시계 → 20:06(실제 20:07).
      시뮬레이터 위치를 서울로 옮기고 팁 재확인 → "일몰은 19:29, 그때 구름 95%".

### 누르고 떼기 + 햅틱 (2026-08-12)
"탭할 때 햅틱 피드백 좀 줘 그리고 탭 했을 때 원 좀 확대 되고 손을 땔 때 위치를 기준으로
하늘이 선택되도록 해줘."

- [x] 구슬마다 붙어 있던 `.onTapGesture` 제거 → 판 전체에 `DragGesture(minimumDistance: 0)`
      하나. `onChanged`로 손가락 아래 구슬을 추적하고 `onEnded`의 위치로 선택.
- [x] 판 밖에서 떼면 선택 안 됨(누른 것 되돌리기).
- [x] 좌표→구슬을 뷰에서 떼어내 `SkyBoard.slot(at:diameter:gap:)`로. 격자가 고정 간격이라
      나눗셈이면 되고, 뷰 밖에 있으니 앱 없이 검증 가능.
- [x] 검증(`scratchpad/hittest`): 구슬 안쪽·여백·둘째 줄 첫 칸·47번·판 밖 3방향 전부 기대값.
      여백은 앞 구슬 몫(죽은 띠 없음).
- [x] 누른 구슬 1.3배 + zIndex 최상단. 엄지가 덮는 것을 옆에서 읽게.
- [x] 햅틱: 구슬 지날 때 `.selection`, 뗄 때 `.impact(.light)`, 셔터 `.impact(.medium)`,
      화면 전환 `.selection`. 전부 `.sensoryFeedback`.
- [x] 확대 렌더링 육안 확인(임시로 한 구슬을 누른 상태로 두고 스크린샷, 훅 제거).
- [ ] **햅틱 자체는 미확인** — 시뮬레이터에는 햅틱 하드웨어가 없어 `.sensoryFeedback`이
      무동작. 실기기에서 확인 필요.

### 수집 축을 시각 → 색으로 (2026-08-12)
"48칸은 오늘 하늘이 보일 색이고 내가 모은색이 이 원 안에 있으면 채워주는거야 … 날짜에 따라서는
48칸이 비어있을 수도 차 있을 수도 있어 … 그건 리스트 화면에서 내가 모은 팔레트 보기로
해결해줘." 그전까지 앱은 **시계**로 자리를 정했음(내가 8/11에 그렇게 만들었고 색 비교 코드는
전부 지웠음).

- [x] `ColorScience/DeltaE.swift` 복구 — 예전 커밋의 CIEDE2000 그대로. Lab 단순 거리는
      파랑 차이를 과장해서 하늘에 부적합.
- [x] `Models/SkyMatch.swift` — 오늘의 48색 각각에 대해 컬렉션에서 ΔE ≤ 반지름인 것 중
      가장 가까운 것(동점이면 최근). 반지름 상수 하나(`SkyMatch.radius = 3.5`).
- [x] `SkyMatchCache` — 목표 색은 날짜·위치·예보가 바뀔 때만 움직이므로 그때만 재계산.
      드래그 프레임마다 48×N번 ΔE를 돌리지 않기 위해.
- [x] `SkyEntry.slot` 삭제. 사진은 자리에 소속되지 않음.
- [x] 촬영 직후 펄스도 색 기준 — 오늘 어느 색에도 안 가까우면 펄스할 자리가 없음(사진은 저장됨).
- [x] 지금 시각 표시 = "이 분의 예상 하늘색에 가장 가까운 구슬" = 지금 나가면 얻는 색.
- [x] 자리 피드 = 그 색의 원 안에 있는 모든 사진, 최근 순. 제목에 색 점 + 구간·시각.
- [x] 세그먼트를 `.topBarTrailing` → `.principal`로. 오른쪽 끝에 밀린 세그먼트는 설정처럼
      읽히고, 폭도 툴바가 주는 만큼 늘어나 큰 제목 위에 걸쳐 있었음. 가운데 정렬 + 폭 190
      고정, 큰 제목은 제거(같은 단어를 두 번 말하고 있었음).
- [x] **기록 화면에 팔레트 보기 추가** — 모은 색 전부를 하루 스펙트럼 순서로.
      정렬은 고정 곡선 기준(`SkyDay.spectrumPosition`)이라 매일 뒤섞이지 않음.
      오늘 판에 안 나오는 사진도 여기서는 반드시 보임.
- [x] 30장 상태로 판·팔레트 육안 확인. 한밤은 다 차고 노을은 대부분 빔(그 금색을 아직 안 가짐).
- [x] 팔레트 재디자인 — "너무 밍숭맹숭하다". 흩어진 동그라미 격자 → 구간별로 묶은 스와치 바
      (간격 3, 모서리 5, 테두리 없음, 구간명 + 개수). 8개 이하는 한 줄이 폭을 채우고, 더
      많으면 마지막 줄이 가장 꽉 차게 나눔(6+1로 매달리던 것 해소). 높이는 전 구간 64로 통일
      (2개짜리 구간이 정사각형 규칙 때문에 두 배 크기로 튀던 것 해소).
      묶는 기준은 **색이 속한 구간**, 찍힌 시각이 아님.
- [ ] **반지름 3.5는 제가 고른 값** — 이웃 구슬 중앙값 3.2 근처로 잡았음. 써 보시고
      "너무 잘 찬다/안 찬다" 느낌으로 조정. 숫자 하나만 바꾸면 됨.
- [ ] 한 색이 여러 구슬을 덮는 것(평균 4자리)이 눈에 거슬리는지도 써 봐야 앎. 거슬리면
      "가장 가까운 한 자리만" 규칙으로 바꿀 수 있지만 그건 "원 안에 있으면"과 다름.

### 남은 의문
- [ ] 흐려짐 기준값(7일/56일/62%)은 눈으로 고른 값. 실제로 몇 주 써 봐야 함.
- [ ] 시드 데이터엔 사진이 없어 피드 카드가 색 블록으로 보임. 실기기 사진으로는 미확인.

### 오늘 찍은 하늘은 그 시간 칸을 가진다 (2026-08-17)
"지금 하늘을 찍었을 때 그리드에 색상이 없어도 괜찮아, 그 원을 내가 찍은 색으로 채워줘 …
예상되는 하늘이지만 사용자가 직접 찍은 하늘은 그 시간대에 하늘의 색이 맞잖아."
구슬의 색은 **예보**고 사진은 **실제**다. 어긋나면 사진이 이긴다.

- [x] `SkyMatch.claims(_:slot:on:)` — 오늘 찍은 사진은 찍힌 반시간 칸을 색과 무관하게 가짐.
      어제 것은 해당 없음(다른 하늘이므로 색 비교만).
- [x] `SkyMatch.map(targets:entries:on:)` 2패스 — 색으로 채운 뒤 오늘 것을 그 위에 덮음.
      한 칸에 둘이면 최근 것이 가짐(다시 찍으면 교체).
- [x] `SkyMatch.all(near:in:filling:on:)` — 구슬을 채운 규칙이 구슬을 열었을 때의 규칙과
      같아야 하므로 피드도 오늘 그 칸의 사진을 포함.
- [x] 지금 시각 표시를 색 최근접 → **시계**로. 찍으면 채워질 자리가 곧 지금 칸이므로
      고리는 "곧 바뀔 구슬"을 가리킴.
- [x] 촬영 직후 펄스도 시계 기준. 예보와 다른 하늘도 이제 펄스할 자리가 반드시 있음
      (예전엔 ΔE 3.5 밖이면 저장만 되고 판에서 아무 일도 안 일어났음).
      단 오늘 찍은 것만 — 라이브러리가 원본 날짜를 갖기 시작하면 거짓말이 되므로.
- [x] 시뮬레이터 검증(21:01): 오늘 지금 시각에 주황(#FF7A00) → 맨 아랫줄 첫 칸이 주황으로
      꽉 참 + 지금 고리. 오늘 3시간 전 초록 → 노을 첫 칸이 초록. 어제 자홍 → 아무 칸도 안 참.
      *(검증용 시드 훅은 제거함. 그 과정에서 시뮬레이터 저장소를 한 번 비웠음.)*

### 찍은 하늘에 이름 붙이기 (2026-08-17)
"내가 찍은 하늘색의 이름을 알려줘." 한국 전통색 · 和色 · 안료 · 표준색(CSS/X11) 네 계열을
한 표에 모으고, CIEDE2000으로 가장 가까운 이름을 사진 카드에 보여준다.

- [x] `ColorScience/SkyName.swift` — 이름 122개. 색값은 전부 출처에서 확인
      (전통색 표, 和色大辞典, 안료별 위키백과 항목, CSS/X11 키워드). 각 이름에 출신과
      한 줄 설명. 팬톤은 색값이 공개 자료가 아니라 표준색 계열을 CSS 키워드로 대체.
- [x] `SkyNames.nearest(to:)` → `(name, distance)`. ΔE ≤ 5면 "이 하늘의 이름",
      넘으면 "가장 가까운 이름"으로 문구가 바뀐다. 122개가 모든 하늘에 정확히 떨어질 수
      없으므로 그 차이를 숨기지 않는다.
- [x] `PhotoDetailView`에 이름 블록 — 카드 바로 아래, 읽는 크기로. hex는 그대로 작은 글씨.
- [x] **표 세 번 고침.** 처음 59개(채도 높은 이름만)로는 22개 표본 중 5개만 ΔE ≤ 5였고
      일출이 13.9, 노을 시작이 15.4였다. 실제 하늘은 표의 색들보다 훨씬 탁하고 옅다.
      옅은 청회색(舛花色·薄花色·라이트 스틸 블루), 모래빛(砂色·榛色·소색·탠),
      흐린 회색(灰青·薄墨色), 아주 어두운 남색(鉄紺·藍鉄), 탁한 벽돌·자주(煉瓦色·小豆色·
      紫鳶)를 채워 122개로. 결과: 11/22가 ΔE ≤ 5, 최악 9.1.
- [x] 검증(`scratchpad/namecheck`): 앱의 하늘 곡선 앵커 22색 각각에 붙는 이름과 ΔE를
      출력. 중복 hex·중복 이름 없음. 뷰 없이 돌아가므로 표를 고칠 때마다 다시 돌릴 수 있음.
- [x] 시뮬레이터에서 카드 육안 확인 — 가까운 경우(하이아오 ΔE 2.4)와 먼 경우
      (우스하나이로 ΔE 6.3) 문구가 각각 맞게 나옴. *(임시 훅 제거함.)*
- [ ] 남은 구멍: 옅은 중간 파랑(#7FA4CE 근처, 오전·늦은 오후)이 ΔE 9. 우스하나이로는
      너무 어둡고 라이트 스틸 블루는 너무 밝다. 그 사이 이름을 더 찾으면 메워짐.
- [ ] 시아노미터 번호(1789년 소쉬르, 파랑 53단계)는 아직 안 붙였음. 이름과 번호를 같이
      쓰면 수집 감성이 세지지만, 파랑이 아닌 하늘은 번호로 담을 수 없어 보류.
- [ ] 이름을 자리 피드·팔레트에도 보여줄지. 지금은 사진 카드 한 곳에만 있음.

### 지금 고리가 늦게 움직이던 것 (2026-08-18)
"원이 가리키는 시간이 지금이 아닐 때가 있는 것 같다." 자리 계산은 멀쩡했고 화면을
갱신하는 주기가 문제였다.

- [x] 자리 계산 전수 검사(`scratchpad/slotcheck`) — 48칸이 0분부터 1440분까지 틈도 겹침도
      없이 덮고, 1440개 분 전부가 자기를 품는 칸으로 떨어지며, 칸의 구간명과 `band()`가
      모두 일치. 어긋남 0건. 즉 `slot(forMinute:)`은 결백.
- [x] **범인은 `.periodic(from: .now, by: 60)`.** 60초마다가 아니라 *판이 열린 순간부터*
      60초마다였다. 21:00:47에 열면 갱신이 21:01:47, 21:02:47… 이라서 21:30 칸으로
      고리가 넘어가는 것이 21:30:47이 된다 — 가리키는 칸이 이미 끝난 뒤까지 최대 1분.
      게다가 `from: .now`는 body가 다시 돌 때마다(예보 도착, 촬영 착지) 다시 읽히므로
      그 오차가 매번 새로운 초로 다시 잡혔다. → `.everyMinute`(분 경계에서 발화)로 교체.
- [x] 앱이 뒤로 갔다 오면 타임라인을 다시 세우도록(`scenePhase` → `.id`). 스케줄은 앱이
      없는 동안 돌지 않으므로, 돌아온 판은 떠날 때 그려진 판이다. 사람이 고리를 보는
      순간이 정확히 그때다.
- [x] 시뮬레이터 확인: 08:19에 3행 1열(08:00–08:40 칸)에 고리. 맞다.

### 구슬을 누르면 그 하늘이 판 밑에 깔린다 (2026-08-18)
"원 누르면 그 때 찍은 사진이 배경으로 깔리게 해줘. 모달 뜨는 거 너무 촌스럽다."
그리고 "팔레트 말고 홈에서도 하늘색 설명 넣어줘." 두 요구는 같은 자리에서 만난다 —
누른 구슬의 사진이 배경이 되고, 그 위에 그 하늘의 이름이 쓰인다.

- [x] 구슬 탭 → `TimelineFeedView` 전체화면 대신 `ReadingSky` 상태. 사진이 화면 전체
      배경(안전영역 밖까지), 나머지 구슬은 0.2로 물러나고 누른 구슬만 제 색·흰 테두리·1.22배.
      화면은 그대로. 나가는 길은 같은 구슬 다시 누르기 / 다른 구슬 / 판의 빈 곳 아무 데나.
- [x] 하늘색 이름을 홈에. `SkyNames.nearest` — 이름 + 출신 + 한 줄 뜻 + 시각·구간·hex.
      ΔE 5 안팎으로 "이 하늘의 이름"과 "가장 가까운 이름"이 갈리는 것은 사진 카드와 동일.
      이름 블록을 누르면 사진 카드(한 줄 쓰기)로, "이 색 N장"을 누르면 그 색의 피드로.
      구슬이 더 이상 피드를 열지 않으므로 피드는 여기서 이름으로 부른다.
- [x] 글자는 누른 구슬의 **반대쪽 끝**에. 위쪽 구슬이면 아래, 아래쪽이면 위 —
      설명이 설명 대상 위에 얹히지 않도록. 아래쪽일 때는 셔터를 피해 88pt 띄움
      (판은 바 밑까지 이어져 있어서 레이아웃이 알아서 피해주지 않음).
- [x] 어둡게 깔기: 검정 0.28 + 글자 쪽으로 0.55 그라디언트. 하늘 사진 위에 하늘색 구슬을
      올리는 일이라 이게 없으면 옅은 구슬이 옅은 구름에 먹힌다. 바의 그라디언트도
      사진 위에서는 흰색이 아니라 검정으로 바뀜(안 그러면 사진 아래에 흰 띠가 생김).
- [x] 빈 구슬 시트에도 그 시각 기준색의 이름과 뜻 한 줄 추가.
- [x] 시뮬레이터 육안 확인 — 오전 05:30(글자 아래) / 노을 19:00(글자 위) 두 경우.
      *(확인용으로 배경을 강제로 켜는 임시 훅과 시드를 썼고, 둘 다 제거함.)*

### 되풀이 (2026-08-18)
"내 일상은 일주일은 한 달은 반복되지만 하늘의 색은 반복되지 않는다는 것을 시각화해서
보여주고 싶어." 기록 화면 세 번째 칸.

- [x] `RepeatView` — 한 줄이 하루의 반 시간. 여러 날에 걸쳐 그 시각에 선 날들의 하늘을
      가로로 늘어놓고 아래에 날짜. **줄이 반복이고 색이 반박**이라 문장으로 잇지 않아도 된다.
- [x] 같은 날 다시 찍은 것은 마지막 것으로 접는다. 하루가 자기 자신을 반복할 수는 없고,
      판이 반 시간을 덮어쓰는 규칙과도 같아야 하므로.
- [x] 맨 아래에 수치 하나 — 같은 시각에 찍힌 모든 쌍 중 가장 가까운 두 하늘의 CIEDE2000.
      ΔE 1 미만이면 "눈으로는 같은 색입니다"로 문장이 바뀐다. "한 번도 같지 않았다"를
      말하려면 가장 비슷했던 값을 옆에 적어야 구호가 아니라 관측이 된다.
- [x] 세그먼트 폭 190 → 250 (기록 · 팔레트 · 되풀이).
- [x] 시뮬레이터 육안 확인 — 4일치 시드로 두 줄 + 가장 비슷한 쌍(ΔE 0.4, 문장 분기 동작).

## 확인 못 한 것
- [ ] 탭 동작 자체는 시뮬레이터에서 손으로 눌러보지 못했음(스크립트로 터치를 넣을 방법이
      없어 배경 상태를 코드로 강제해 화면만 확인). 같은 구슬 다시 누르기 / 빈 곳 눌러 닫기 /
      이름 블록 눌러 카드 열기 세 경로는 실기기에서 확인할 것.
- [ ] 프리뷰 `resizeAspectFill` 영역과 중앙 크롭이 실기기에서 실제로 일치하는지.
- [ ] 뒤로 갔다 온 뒤의 갱신은 시뮬레이터에서 재현해 보지 못했음(분 단위 대기가 필요).
      `.id` 재생성은 확실히 새 시각을 읽지만, 그것이 없었을 때 실제로 낡은 채로 있었는지는
      미확인.
- [ ] 촬영 직후 채워진 구슬의 펄스 애니메이션 (`landed`) 육안 확인.

## 다음에 할 것 (README '다음에 손댈 것'에서)
- [ ] 락스크린 위젯 — 오늘 찍은 하늘 그대로
- [ ] 판 마흔여덟 칸 전체를 이미지 한 장으로 내보내기
- [ ] 라이브러리에서 고른 사진은 `PHAsset.creationDate`를 촬영 시각으로 쓰기
      (지금은 `Date()`라 어제 노을이 오늘 밤 칸에 들어감)
- [ ] 오래된 원본 정리 옵션 (썸네일과 색만 남기기)
